name: release

on:
  # run when a release has been created
  release:
    types: [created]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Testing
        run: echo "Release just happened"

