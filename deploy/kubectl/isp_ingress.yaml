apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: isp-app
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: isp-app
                port:
                  number: 8000
            path: /
            pathType: Prefix
